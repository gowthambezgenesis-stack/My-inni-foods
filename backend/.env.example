SECRET_KEY=change-me-to-a-random-secret-key
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1

DB_NAME=inni_db
DB_USER=postgres
DB_PASSWORD=your-postgres-password
DB_HOST=localhost
DB_PORT=5432

# Razorpay (get keys from https://dashboard.razorpay.com/app/keys)
RAZORPAY_KEY_ID=rzp_test_xxxxxxxxxxxx
RAZORPAY_KEY_SECRET=your-razorpay-secret-key
RAZORPAY_WEBHOOK_SECRET=your-webhook-secret

# CORS
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000

# SendGrid HTTP API — required for OTP, order, contact, and partner emails on Railway
# Create a key at https://app.sendgrid.com/settings/api_keys (Mail Send permission)
# Authenticate your domain (CNAME records) at Sender Authentication → Domain Authentication
SENDGRID_API_KEY=SG.xxxxxxxx
DEFAULT_FROM_EMAIL=INNI Foods <noreply@your-verified-domain.com>

# Optional: print emails to the console instead of sending (local dev only)
# EMAIL_BACKEND=django.core.mail.backends.console.EmailBackend

# New order admin notifications
ORDER_NOTIFICATIONS_ENABLED=True
FRONTEND_BASE_URL=https://www.innifoods.com
# Links in admin new-order emails (defaults to FRONTEND_BASE_URL if unset)
# Must be ONE url only — do not paste comma-separated values
ADMIN_PORTAL_BASE_URL=https://www.innifoods.com
# Customer order-success emails use the same ORDER_NOTIFICATIONS_ENABLED + SendGrid settings

# Twilio WhatsApp — customer order confirmation after payment (optional)
# Sandbox: join your phone in Twilio Console → Messaging → Try WhatsApp
# API_PUBLIC_BASE_URL: public HTTPS URL so Twilio can attach invoice PDF (e.g. ngrok tunnel to :8000)
WHATSAPP_NOTIFICATIONS_ENABLED=False
API_PUBLIC_BASE_URL=
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_WHATSAPP_NUMBER=whatsapp:+14155238886

# OpenAPI / Swagger UI (enabled by default when DEBUG=True)
SWAGGER_ENABLED=True

# Meilisearch — typo-tolerant admin order search (run: meilisearch --master-key=devMasterKey)
# Then: python manage.py sync_meilisearch_orders
MEILISEARCH_URL=http://127.0.0.1:7700
MEILISEARCH_API_KEY=devMasterKey

